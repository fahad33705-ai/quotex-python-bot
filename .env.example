# Do NOT put real credentials in source code.
# Fill these locally only if the community Quotex client you use requires them.
QUOTEX_EMAIL=
QUOTEX_PASSWORD=
QUOTEX_ASSET=EURUSD
CANDLE_SECONDS=60
HISTORY_SIZE=200
